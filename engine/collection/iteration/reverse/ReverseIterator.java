package engine.collection.iteration.reverse;

import engine.collection.iteration.Iterator;

/**
 * An {@link Iterator} that starts at the end of a collection
 *
 * @param <T> type of elements iterating over
 */
public interface ReverseIterator<T> extends Iterator<T> {
}
